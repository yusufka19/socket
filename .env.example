# Node.js WebSocket Server for Football Match Game

PORT=8080
NODE_ENV=development

# Optional: Database connection (for future use)
# DATABASE_URL=mongodb://localhost:27017/football_match_game

# Optional: Redis for session storage (for scaling)
# REDIS_URL=redis://localhost:6379

# CORS settings
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
